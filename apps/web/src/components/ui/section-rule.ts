/**
 * A quiet hairline separates un-carded sections such as magazine spread and
 * form headings. Bordered cards already provide their own frame.
 */
export const sectionRuleClass = "border-foreground border-t pt-1.5";
