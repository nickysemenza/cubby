// Generated by `pnpm generate` — do not edit.

// Artifact hashes: source=f096729c1780bd07 content=ddd3a76cd563f5be
// Generated schema aliases retain deterministic import order.
import { productListItemOut as productListOutputSchema } from "@cubby/schemas/product";
import { recipeListItemOut as recipeListOutputSchema } from "@cubby/schemas/recipe";
import { ingredientListItemOut as ingredientListOutputSchema } from "@cubby/schemas/ingredient";
import { locationListItemOut as locationListOutputSchema } from "@cubby/schemas/location";
import { inventoryListItemOut as inventoryListOutputSchema } from "@cubby/schemas/inventory";
import { mealListItemOut as mealListOutputSchema } from "@cubby/schemas/meal";
import { ledgerPartyOut as ledgerPartyListOutputSchema } from "@cubby/schemas/ledger-party";
import { ledgerTransferOut as ledgerTransferListOutputSchema } from "@cubby/schemas/ledger-transfer";
import { projectListItemOut as projectListOutputSchema } from "@cubby/schemas/project";
import { taskListItemOut as taskListOutputSchema } from "@cubby/schemas/project";
import { vendorOut as vendorListOutputSchema } from "@cubby/schemas/vendor";
import { purchaseListItemOut as purchaseListOutputSchema } from "@cubby/schemas/purchase";
import { financialAccountOut as financialAccountListOutputSchema } from "@cubby/schemas/financial-account";
import { financialTransactionOut as financialTransactionListOutputSchema } from "@cubby/schemas/financial-transaction";
import { wishListItemOut as wishListOutputSchema } from "@cubby/schemas/wish";
import { expenseListItemOut as expenseListOutputSchema } from "@cubby/schemas/project";
import { plantingListItemOut as plantingListOutputSchema } from "@cubby/schemas/planting";
import { gardenEntryListItemOut as gardenEntryListOutputSchema } from "@cubby/schemas/garden-entry";
import { vendorAccountOut as vendorAccountListOutputSchema } from "@cubby/schemas/vendor-account";
import { productCategoryOut as productCategoryListOutputSchema } from "@cubby/schemas/product-category";
import { productFilterFields as productListFilterFields } from "@cubby/schemas/product";
import { recipeFilterFields as recipeListFilterFields } from "@cubby/schemas/recipe";
import { ingredientFilterFields as ingredientListFilterFields } from "@cubby/schemas/ingredient";
import { locationFilterFields as locationListFilterFields } from "@cubby/schemas/location";
import { inventoryFilterFields as inventoryListFilterFields } from "@cubby/schemas/inventory";
import { mealFilterFields as mealListFilterFields } from "@cubby/schemas/meal";
import { ledgerPartyFilterFields as ledgerPartyListFilterFields } from "@cubby/schemas/ledger-party";
import { ledgerTransferFilterFields as ledgerTransferListFilterFields } from "@cubby/schemas/ledger-transfer";
import { projectFilterFields as projectListFilterFields } from "@cubby/schemas/project";
import { taskFilterFields as taskListFilterFields } from "@cubby/schemas/project";
import { vendorFilterFields as vendorListFilterFields } from "@cubby/schemas/vendor";
import { purchaseFilterFields as purchaseListFilterFields } from "@cubby/schemas/purchase";
import { financialAccountFilterFields as financialAccountListFilterFields } from "@cubby/schemas/financial-account";
import { financialTransactionFilterFields as financialTransactionListFilterFields } from "@cubby/schemas/financial-transaction";
import { wishFilterFields as wishListFilterFields } from "@cubby/schemas/wish";
import { expenseFilterFields as expenseListFilterFields } from "@cubby/schemas/project";
import { plantingFilterFields as plantingListFilterFields } from "@cubby/schemas/planting";
import { gardenEntryFilterFields as gardenEntryListFilterFields } from "@cubby/schemas/garden-entry";
import { vendorAccountFilterFields as vendorAccountListFilterFields } from "@cubby/schemas/vendor-account";
import { productCategoryFilterFields as productCategoryListFilterFields } from "@cubby/schemas/product-category";
import { withEntityListMedia } from "@cubby/schemas/entity-read-media";
import {
  MAX_PAGE_SIZE,
  MAX_SORTS,
  paginatedMetaSchema,
} from "@cubby/schemas/pagination";
import type { FilterPatch } from "../filters";
import { z } from "zod";

export const listEntities = [
  "product",
  "recipe",
  "ingredient",
  "location",
  "inventory",
  "meal",
  "ledgerParty",
  "ledgerTransfer",
  "project",
  "task",
  "vendor",
  "purchase",
  "financialAccount",
  "financialTransaction",
  "wish",
  "expense",
  "planting",
  "gardenEntry",
  "vendorAccount",
  "productCategory",
] as const;
export type ListEntity = (typeof listEntities)[number];

const entityListSortSchema = z.object({
  orderBy: z.string().min(1),
  direction: z.enum(["asc", "desc"]),
});
const entityListSortsSchema = z.union([
  entityListSortSchema,
  z.array(entityListSortSchema).min(1).max(MAX_SORTS),
]);
const entityListPaginationSchema = z.object({
  pageIndex: z.number().int().min(0),
  pageSize: z.number().int().min(1).max(MAX_PAGE_SIZE),
});
// The one page-metadata schema every list shares (one OpenAPI component).
const entityListMetaSchema = paginatedMetaSchema;

const productListFiltersSchema = z
  .object(productListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const recipeListFiltersSchema = z
  .object(recipeListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const ingredientListFiltersSchema = z
  .object(ingredientListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const locationListFiltersSchema = z
  .object(locationListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const inventoryListFiltersSchema = z
  .object(inventoryListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const mealListFiltersSchema = z
  .object(mealListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const ledgerPartyListFiltersSchema = z.object(ledgerPartyListFilterFields);
const ledgerTransferListFiltersSchema = z.object(
  ledgerTransferListFilterFields,
);
const projectListFiltersSchema = z
  .object(projectListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const taskListFiltersSchema = z
  .object(taskListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const vendorListFiltersSchema = z
  .object(vendorListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const purchaseListFiltersSchema = z
  .object(purchaseListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const financialAccountListFiltersSchema = z
  .object(financialAccountListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const financialTransactionListFiltersSchema = z
  .object(financialTransactionListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const wishListFiltersSchema = z
  .object(wishListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const expenseListFiltersSchema = z
  .object(expenseListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const plantingListFiltersSchema = z
  .object(plantingListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const gardenEntryListFiltersSchema = z
  .object(gardenEntryListFilterFields)
  .extend({ searchQuery: z.string().trim().min(1).max(100).optional() });
const vendorAccountListFiltersSchema = z.object(vendorAccountListFilterFields);
const productCategoryListFiltersSchema = z.object(
  productCategoryListFilterFields,
);

// One generated filter schema per list entity.
// oxfmt-ignore
const ENTITY_LIST_FILTER_SCHEMAS = {
  "product": productListFiltersSchema,
  "recipe": recipeListFiltersSchema,
  "ingredient": ingredientListFiltersSchema,
  "location": locationListFiltersSchema,
  "inventory": inventoryListFiltersSchema,
  "meal": mealListFiltersSchema,
  "ledgerParty": ledgerPartyListFiltersSchema,
  "ledgerTransfer": ledgerTransferListFiltersSchema,
  "project": projectListFiltersSchema,
  "task": taskListFiltersSchema,
  "vendor": vendorListFiltersSchema,
  "purchase": purchaseListFiltersSchema,
  "financialAccount": financialAccountListFiltersSchema,
  "financialTransaction": financialTransactionListFiltersSchema,
  "wish": wishListFiltersSchema,
  "expense": expenseListFiltersSchema,
  "planting": plantingListFiltersSchema,
  "gardenEntry": gardenEntryListFiltersSchema,
  "vendorAccount": vendorAccountListFiltersSchema,
  "productCategory": productCategoryListFiltersSchema,
} as const;

export const entityListInputSchema = z.discriminatedUnion("entity", [
  z.object({
    entity: z.literal("product"),
    filters: productListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("recipe"),
    filters: recipeListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("ingredient"),
    filters: ingredientListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("location"),
    filters: locationListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("inventory"),
    filters: inventoryListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("meal"),
    filters: mealListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("ledgerParty"),
    filters: ledgerPartyListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("ledgerTransfer"),
    filters: ledgerTransferListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("project"),
    filters: projectListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("task"),
    filters: taskListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("vendor"),
    filters: vendorListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("purchase"),
    filters: purchaseListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("financialAccount"),
    filters: financialAccountListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("financialTransaction"),
    filters: financialTransactionListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("wish"),
    filters: wishListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("expense"),
    filters: expenseListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("planting"),
    filters: plantingListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("gardenEntry"),
    filters: gardenEntryListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("vendorAccount"),
    filters: vendorAccountListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("productCategory"),
    filters: productCategoryListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
]);

export const productListItem = withEntityListMedia(productListOutputSchema);
export const recipeListItem = withEntityListMedia(recipeListOutputSchema);
export const ingredientListItem = withEntityListMedia(
  ingredientListOutputSchema,
);
export const locationListItem = withEntityListMedia(locationListOutputSchema);
export const inventoryListItem = withEntityListMedia(inventoryListOutputSchema);
export const mealListItem = withEntityListMedia(mealListOutputSchema);
export const ledgerPartyListItem = withEntityListMedia(
  ledgerPartyListOutputSchema,
);
export const ledgerTransferListItem = withEntityListMedia(
  ledgerTransferListOutputSchema,
);
export const projectListItem = withEntityListMedia(projectListOutputSchema);
export const taskListItem = withEntityListMedia(taskListOutputSchema);
export const vendorListItem = withEntityListMedia(vendorListOutputSchema);
export const purchaseListItem = withEntityListMedia(purchaseListOutputSchema);
export const financialAccountListItem = withEntityListMedia(
  financialAccountListOutputSchema,
);
export const financialTransactionListItem = withEntityListMedia(
  financialTransactionListOutputSchema,
);
export const wishListItem = withEntityListMedia(wishListOutputSchema);
export const expenseListItem = withEntityListMedia(expenseListOutputSchema);
export const plantingListItem = withEntityListMedia(plantingListOutputSchema);
export const gardenEntryListItem = withEntityListMedia(
  gardenEntryListOutputSchema,
);
export const vendorAccountListItem = withEntityListMedia(
  vendorAccountListOutputSchema,
);
export const productCategoryListItem = withEntityListMedia(
  productCategoryListOutputSchema,
);

const ENTITY_LIST_OUTPUT_SCHEMAS = {
  product: z.object({
    items: z.array(productListItem),
    meta: entityListMetaSchema,
  }),
  recipe: z.object({
    items: z.array(recipeListItem),
    meta: entityListMetaSchema,
  }),
  ingredient: z.object({
    items: z.array(ingredientListItem),
    meta: entityListMetaSchema,
  }),
  location: z.object({
    items: z.array(locationListItem),
    meta: entityListMetaSchema,
  }),
  inventory: z.object({
    items: z.array(inventoryListItem),
    meta: entityListMetaSchema,
  }),
  meal: z.object({ items: z.array(mealListItem), meta: entityListMetaSchema }),
  ledgerParty: z.object({
    items: z.array(ledgerPartyListItem),
    meta: entityListMetaSchema,
  }),
  ledgerTransfer: z.object({
    items: z.array(ledgerTransferListItem),
    meta: entityListMetaSchema,
  }),
  project: z.object({
    items: z.array(projectListItem),
    meta: entityListMetaSchema,
  }),
  task: z.object({ items: z.array(taskListItem), meta: entityListMetaSchema }),
  vendor: z.object({
    items: z.array(vendorListItem),
    meta: entityListMetaSchema,
  }),
  purchase: z.object({
    items: z.array(purchaseListItem),
    meta: entityListMetaSchema,
  }),
  financialAccount: z.object({
    items: z.array(financialAccountListItem),
    meta: entityListMetaSchema,
  }),
  financialTransaction: z.object({
    items: z.array(financialTransactionListItem),
    meta: entityListMetaSchema,
  }),
  wish: z.object({ items: z.array(wishListItem), meta: entityListMetaSchema }),
  expense: z.object({
    items: z.array(expenseListItem),
    meta: entityListMetaSchema,
  }),
  planting: z.object({
    items: z.array(plantingListItem),
    meta: entityListMetaSchema,
  }),
  gardenEntry: z.object({
    items: z.array(gardenEntryListItem),
    meta: entityListMetaSchema,
  }),
  vendorAccount: z.object({
    items: z.array(vendorAccountListItem),
    meta: entityListMetaSchema,
  }),
  productCategory: z.object({
    items: z.array(productCategoryListItem),
    meta: entityListMetaSchema,
  }),
} as const;

type EntityListInputFiltersByEntity = {
  [E in ListEntity]: z.input<(typeof ENTITY_LIST_FILTER_SCHEMAS)[E]>;
};
type EntityListParsedFiltersByEntity = {
  [E in ListEntity]: z.output<(typeof ENTITY_LIST_FILTER_SCHEMAS)[E]>;
};
type EntityListSort = z.input<typeof entityListSortSchema>;
export type EntityListInputByEntity = {
  [E in ListEntity]: {
    entity: E;
    filters: EntityListInputFiltersByEntity[E];
    sort?: EntityListSort | EntityListSort[];
    pagination?: { pageIndex: number; pageSize: number };
    groupBy?: string;
  };
};
export type ParsedEntityListInputByEntity = {
  [E in ListEntity]: Omit<EntityListInputByEntity[E], "filters"> & {
    filters: EntityListParsedFiltersByEntity[E];
  };
};

export type EntityListParamsByEntity = {
  [E in ListEntity]: Omit<EntityListInputByEntity[E], "entity">;
};
export function entityListInputFor<E extends ListEntity>(
  entity: E,
  input: EntityListParamsByEntity[E],
): EntityListInputByEntity[E];
export function entityListInputFor(
  entity: ListEntity,
  input: EntityListParamsByEntity[ListEntity],
) {
  return { entity, ...input };
}

export function entityListParamsFromParsed<E extends ListEntity>(
  entity: E,
  input: ParsedEntityListInputByEntity[E],
): EntityListParamsByEntity[E];
export function entityListParamsFromParsed(
  entity: ListEntity,
  input: ParsedEntityListInputByEntity[ListEntity],
) {
  if (input.entity !== entity)
    throw new Error("Entity list input discriminator mismatch");
  const { entity: _entity, ...params } = input;
  return params;
}

export type EntityListParseInput =
  | EntityListInputByEntity[ListEntity]
  | {
      entity: ListEntity;
      filters: FilterPatch;
      sort?: EntityListSort | EntityListSort[];
      pagination?: { pageIndex: number; pageSize: number };
      groupBy?: string;
    };

export type EntityListResultByEntity = {
  [E in ListEntity]: z.output<(typeof ENTITY_LIST_OUTPUT_SCHEMAS)[E]>;
};

export function parseEntityListInput<E extends ListEntity>(
  entity: E,
  value: EntityListInputByEntity[E] | EntityListParseInput,
): ParsedEntityListInputByEntity[E];
export function parseEntityListInput(
  entity: ListEntity,
  value: EntityListParseInput,
) {
  const parsed = entityListInputSchema.parse(value);
  if (parsed.entity !== entity)
    throw new Error("Entity list input discriminator mismatch");
  return parsed;
}

export function getEntityListOutputSchema<E extends ListEntity>(
  entity: E,
): z.ZodType<EntityListResultByEntity[E]>;
export function getEntityListOutputSchema(entity: ListEntity): z.ZodType {
  return ENTITY_LIST_OUTPUT_SCHEMAS[entity];
}
