// Generated by `pnpm entity:generate` from `scripts/entity-literals/entities/*.entity.ts`. Do not edit.

// Entity artifact hashes: source=ecd6a5334ee1c34d content=ae97d31066decb92
// biome-ignore assist/source/organizeImports: generated schema aliases are deterministic.
import { productListItemOut as productListOutputSchema } from "@cubby/schemas/product";
import { recipeListItemOut as recipeListOutputSchema } from "@cubby/schemas/recipe";
import { ingredientListItemOut as ingredientListOutputSchema } from "@cubby/schemas/ingredient";
import { locationListItemOut as locationListOutputSchema } from "@cubby/schemas/location";
import { inventoryListItemOut as inventoryListOutputSchema } from "@cubby/schemas/inventory";
import { mealOut as mealListOutputSchema } from "@cubby/schemas/meal";
import { projectOut as projectListOutputSchema } from "@cubby/schemas/project";
import { taskOut as taskListOutputSchema } from "@cubby/schemas/project";
import { vendorOut as vendorListOutputSchema } from "@cubby/schemas/vendor";
import { purchaseOut as purchaseListOutputSchema } from "@cubby/schemas/purchase";
import { financialAccountOut as financialAccountListOutputSchema } from "@cubby/schemas/financial-account";
import { financialTransactionOut as financialTransactionListOutputSchema } from "@cubby/schemas/financial-transaction";
import { wishOut as wishListOutputSchema } from "@cubby/schemas/wish";
import { expenseOut as expenseListOutputSchema } from "@cubby/schemas/project";
import { productFilterFields as productListFilterFields } from "@cubby/schemas/product";
import { recipeFilterFields as recipeListFilterFields } from "@cubby/schemas/recipe";
import { ingredientFilterFields as ingredientListFilterFields } from "@cubby/schemas/ingredient";
import { locationFilterFields as locationListFilterFields } from "@cubby/schemas/location";
import { inventoryFilterFields as inventoryListFilterFields } from "@cubby/schemas/inventory";
import { mealFilterFields as mealListFilterFields } from "@cubby/schemas/meal";
import { projectFilterFields as projectListFilterFields } from "@cubby/schemas/project";
import { taskFilterFields as taskListFilterFields } from "@cubby/schemas/project";
import { vendorFilterFields as vendorListFilterFields } from "@cubby/schemas/vendor";
import { purchaseFilterFields as purchaseListFilterFields } from "@cubby/schemas/purchase";
import { financialAccountFilterFields as financialAccountListFilterFields } from "@cubby/schemas/financial-account";
import { financialTransactionFilterFields as financialTransactionListFilterFields } from "@cubby/schemas/financial-transaction";
import { wishFilterFields as wishListFilterFields } from "@cubby/schemas/wish";
import { expenseFilterFields as expenseListFilterFields } from "@cubby/schemas/project";
import { MAX_PAGE_SIZE, MAX_SORTS } from "@cubby/schemas/pagination";
import { z } from "zod";

export const listEntities = [
  "product",
  "recipe",
  "ingredient",
  "location",
  "inventory",
  "meal",
  "project",
  "task",
  "vendor",
  "purchase",
  "financialAccount",
  "financialTransaction",
  "wish",
  "expense",
] as const;
export type ListEntity = (typeof listEntities)[number];

const entityListSortSchema = z.object({
  orderBy: z.string().min(1),
  direction: z.enum(["asc", "desc"]),
});
const entityListSortsSchema = z.union([
  entityListSortSchema,
  z.array(entityListSortSchema).min(1).max(MAX_SORTS),
]);
const entityListPaginationSchema = z.object({
  pageIndex: z.number().int().min(0),
  pageSize: z.number().int().min(1).max(MAX_PAGE_SIZE),
});
const entityListMetaSchema = z.object({
  pageIndex: z.number().int().min(0),
  pageSize: z.number().int().min(1).max(MAX_PAGE_SIZE),
  totalCount: z.number().int().min(0),
  sums: z.record(z.string(), z.number()).optional(),
});

const productListFiltersSchema = z.object(productListFilterFields);
const recipeListFiltersSchema = z.object(recipeListFilterFields);
const ingredientListFiltersSchema = z.object(ingredientListFilterFields);
const locationListFiltersSchema = z.object(locationListFilterFields);
const inventoryListFiltersSchema = z.object(inventoryListFilterFields);
const mealListFiltersSchema = z.object(mealListFilterFields);
const projectListFiltersSchema = z.object(projectListFilterFields);
const taskListFiltersSchema = z.object(taskListFilterFields);
const vendorListFiltersSchema = z.object(vendorListFilterFields);
const purchaseListFiltersSchema = z.object(purchaseListFilterFields);
const financialAccountListFiltersSchema = z.object(
  financialAccountListFilterFields,
);
const financialTransactionListFiltersSchema = z.object(
  financialTransactionListFilterFields,
);
const wishListFiltersSchema = z.object(wishListFilterFields);
const expenseListFiltersSchema = z.object(expenseListFilterFields);

export const entityListInputSchema = z.discriminatedUnion("entity", [
  z.object({
    entity: z.literal("product"),
    filters: productListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("recipe"),
    filters: recipeListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("ingredient"),
    filters: ingredientListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("location"),
    filters: locationListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("inventory"),
    filters: inventoryListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("meal"),
    filters: mealListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("project"),
    filters: projectListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("task"),
    filters: taskListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("vendor"),
    filters: vendorListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("purchase"),
    filters: purchaseListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("financialAccount"),
    filters: financialAccountListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("financialTransaction"),
    filters: financialTransactionListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("wish"),
    filters: wishListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
  z.object({
    entity: z.literal("expense"),
    filters: expenseListFiltersSchema,
    sort: entityListSortsSchema.optional(),
    pagination: entityListPaginationSchema.optional(),
    groupBy: z.string().min(1).optional(),
  }),
]);

const ENTITY_LIST_OUTPUT_SCHEMAS = {
  product: z.object({
    items: z.array(productListOutputSchema),
    meta: entityListMetaSchema,
  }),
  recipe: z.object({
    items: z.array(recipeListOutputSchema),
    meta: entityListMetaSchema,
  }),
  ingredient: z.object({
    items: z.array(ingredientListOutputSchema),
    meta: entityListMetaSchema,
  }),
  location: z.object({
    items: z.array(locationListOutputSchema),
    meta: entityListMetaSchema,
  }),
  inventory: z.object({
    items: z.array(inventoryListOutputSchema),
    meta: entityListMetaSchema,
  }),
  meal: z.object({
    items: z.array(mealListOutputSchema),
    meta: entityListMetaSchema,
  }),
  project: z.object({
    items: z.array(projectListOutputSchema),
    meta: entityListMetaSchema,
  }),
  task: z.object({
    items: z.array(taskListOutputSchema),
    meta: entityListMetaSchema,
  }),
  vendor: z.object({
    items: z.array(vendorListOutputSchema),
    meta: entityListMetaSchema,
  }),
  purchase: z.object({
    items: z.array(purchaseListOutputSchema),
    meta: entityListMetaSchema,
  }),
  financialAccount: z.object({
    items: z.array(financialAccountListOutputSchema),
    meta: entityListMetaSchema,
  }),
  financialTransaction: z.object({
    items: z.array(financialTransactionListOutputSchema),
    meta: entityListMetaSchema,
  }),
  wish: z.object({
    items: z.array(wishListOutputSchema),
    meta: entityListMetaSchema,
  }),
  expense: z.object({
    items: z.array(expenseListOutputSchema),
    meta: entityListMetaSchema,
  }),
} as const;

type EntityListFiltersByEntity = {
  product: z.input<typeof productListFiltersSchema>;
  recipe: z.input<typeof recipeListFiltersSchema>;
  ingredient: z.input<typeof ingredientListFiltersSchema>;
  location: z.input<typeof locationListFiltersSchema>;
  inventory: z.input<typeof inventoryListFiltersSchema>;
  meal: z.input<typeof mealListFiltersSchema>;
  project: z.input<typeof projectListFiltersSchema>;
  task: z.input<typeof taskListFiltersSchema>;
  vendor: z.input<typeof vendorListFiltersSchema>;
  purchase: z.input<typeof purchaseListFiltersSchema>;
  financialAccount: z.input<typeof financialAccountListFiltersSchema>;
  financialTransaction: z.input<typeof financialTransactionListFiltersSchema>;
  wish: z.input<typeof wishListFiltersSchema>;
  expense: z.input<typeof expenseListFiltersSchema>;
};
type EntityListSort = z.input<typeof entityListSortSchema>;
export type EntityListInputByEntity = {
  [E in ListEntity]: {
    entity: E;
    filters: EntityListFiltersByEntity[E];
    sort?: EntityListSort | EntityListSort[];
    pagination?: { pageIndex: number; pageSize: number };
    groupBy?: string;
  };
};

export type EntityListResultByEntity = {
  [E in ListEntity]: z.output<(typeof ENTITY_LIST_OUTPUT_SCHEMAS)[E]>;
};

export function parseEntityListInput<E extends ListEntity>(
  entity: E,
  value: unknown,
): EntityListInputByEntity[E];
export function parseEntityListInput(
  entity: ListEntity,
  value: unknown,
): unknown {
  const parsed = entityListInputSchema.parse(value);
  if (parsed.entity !== entity)
    throw new Error("Entity list input discriminator mismatch");
  return parsed;
}

export function parseEntityListResult<E extends ListEntity>(
  entity: E,
  value: unknown,
): EntityListResultByEntity[E];
export function parseEntityListResult(
  entity: ListEntity,
  value: unknown,
): unknown {
  return ENTITY_LIST_OUTPUT_SCHEMAS[entity].parse(value);
}
