// Generated by `pnpm generate` — do not edit.

// Artifact hashes: source=13ee1cd540ad1b55 content=55958d1e9992c97f
import type { Amount } from "@cubby/schemas/codec";
import type {
  FinancialAccountCardNumber,
  FinancialAccountIdentity,
  FinancialAccountSourceAlias,
} from "@cubby/schemas/financial-account";
import type { FinancialTransactionSourceRef } from "@cubby/schemas/financial-transaction";
import type {
  CookbookId,
  DeviceId,
  ExpenseId,
  FinancialAccountId,
  FinancialTransactionId,
  ImageSightingId,
  ImportRunId,
  IngredientId,
  InventoryId,
  LedgerPartyId,
  LedgerTransferId,
  LocationId,
  MealId,
  PlantId,
  ProductCategoryId,
  ProductId,
  ProjectId,
  PurchaseId,
  RecipeId,
  TaskId,
  VendorAccountId,
  VendorId,
  WishId,
} from "@cubby/schemas/identifiers";
import { imageStatusValues } from "@cubby/schemas/image";
import type {
  ImageSourceFingerprint,
  StoredImageEmbeddedMetadata,
} from "@cubby/schemas/image";
import type {
  ImageCaptureLocation,
  ImageProvenanceEvidence,
} from "@cubby/schemas/image-capture-fields";
import type {
  ImageSightingCamera,
  ImageSightingLocation,
} from "@cubby/schemas/image-sighting-fields";
import type {
  CookbookExtraction,
  CookbookRunReport,
} from "@cubby/schemas/cookbook";
import type { LedgerPartyKind } from "@cubby/schemas/ledger-party";
import {
  mealKindValues,
  mealTypeValues,
} from "@cubby/schemas/meal-classification";
import type { BaseKind } from "@cubby/schemas/problems";
import { productCategoryFeatureValues } from "@cubby/schemas/product-category-fields";
import {
  costTypeValues,
  projectKindValues,
  projectStatusValues,
  taskStatusValues,
  tradeValues,
} from "@cubby/schemas/project";
import {
  expenseLineBasisValues,
  expenseLineKindValues,
} from "@cubby/schemas/expense-line-kind";
import type { ProductLabelNutrition } from "@cubby/schemas/nutrition";
import type {
  RecipeStoredMeta,
  RecipeYield,
  StoredRecipeTotals,
} from "@cubby/schemas/recipe-shared";
import { recipeSourceValues } from "@cubby/schemas/recipe-shared";
import { inventoryOwnershipModeValues } from "@cubby/schemas/inventory-ownership";
import { inventoryPlacementValues } from "@cubby/shared";
import { sql } from "drizzle-orm";
import {
  type AnyPgColumn,
  boolean,
  date,
  doublePrecision,
  integer,
  jsonb,
  pgEnum,
  real,
  text,
  timestamp,
  uuid,
} from "drizzle-orm/pg-core";

export const recipeSourceEnum = pgEnum("RecipeSource", recipeSourceValues);
export const imageStatusEnum = pgEnum("ImageStatus", imageStatusValues);
export const inventoryPlacementEnum = pgEnum(
  "InventoryPlacement",
  inventoryPlacementValues,
);
export const imageRenderStatusEnum = pgEnum("ImageRenderStatus", [
  "unverified",
  "verified",
  "failed",
]);
export const imageStorageStatusEnum = pgEnum("ImageStorageStatus", [
  "unverified",
  "available",
  "missing",
  "metadata_mismatch",
]);

export const generatedProductColumns = (
  references: Readonly<{
    ingredient: () => AnyPgColumn;
    plant: () => AnyPgColumn;
    productCategory: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ProductId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  aliases: text("aliases")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  manufacturer: text("manufacturer").notNull(),
  fdc_id: integer("fdc_id"),
  model: text("model"),
  expectedQuantity: integer("expectedQuantity"),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
  ingredientId: uuid("ingredientId")
    .$type<IngredientId>()
    .references(references["ingredient"]),
  growsPlantId: uuid("growsPlantId")
    .$type<PlantId>()
    .references(references["plant"]),
  categoryId: uuid("categoryId")
    .$type<ProductCategoryId>()
    .references(references["productCategory"]),
  tags: text("tags")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  price: real("price"),
  usdaUnavailable: boolean("usdaUnavailable"),
  stockTracked: boolean("stockTracked"),
  labelNutrition: jsonb("labelNutrition").$type<ProductLabelNutrition | null>(),
});

export const generatedRecipeColumns = (
  references: Readonly<{
    cookbook: () => AnyPgColumn;
    recipe: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<RecipeId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
  SourceType: recipeSourceEnum("SourceType"),
  SourceData: text("SourceData"),
  cookbookId: uuid("cookbookId")
    .$type<CookbookId>()
    .references(references["cookbook"]),
  forkedFromRecipeId: uuid("forkedFromRecipeId")
    .$type<RecipeId>()
    .references(references["recipe"]),
  yield: jsonb("yield").$type<RecipeYield>(),
  servings: integer("servings"),
  tags: text("tags").array(),
  notes: text("notes"),
  totals: jsonb("totals").$type<StoredRecipeTotals | null>(),
  totalsComputedAt: timestamp("totalsComputedAt", { mode: "date" }),
  activeMinutes: integer("activeMinutes"),
  totalMinutes: integer("totalMinutes"),
  meta: jsonb("meta").$type<RecipeStoredMeta | null>(),
});

export const generatedIngredientColumns = (
  references: Readonly<{ recipe: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<IngredientId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  aliases: text("aliases")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  naKinds: text("naKinds")
    .array()
    .$type<BaseKind[]>()
    .notNull()
    .default(sql`'{}'::text[]`),
  usuallyOnHand: boolean("usuallyOnHand").notNull().default(false),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
  recipeId: uuid("recipeId").$type<RecipeId>().references(references["recipe"]),
});

export const generatedCookbookColumns = (
  references: Readonly<{ product: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<CookbookId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  author: text("author")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  subjects: text("subjects")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  sourceLabel: text("sourceLabel").notNull(),
  rawJson: jsonb("rawJson").$type<CookbookExtraction>().notNull(),
  report: jsonb("report").$type<CookbookRunReport | null>(),
  sourceRecipeCount: integer("sourceRecipeCount").notNull().default(0),
  productId: uuid("productId")
    .$type<ProductId>()
    .references(references["product"]),
  importedAt: timestamp("importedAt", { mode: "date" }).notNull().defaultNow(),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedLocationColumns = (
  references: Readonly<{
    location: () => AnyPgColumn;
    product: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<LocationId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  aliases: text("aliases")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  tags: text("tags")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
  lastBulkInventory: timestamp("lastBulkInventory", { mode: "date" }),
  parentId: uuid("parentId")
    .$type<LocationId>()
    .references(references["location"]),
  productId: uuid("productId")
    .$type<ProductId>()
    .references(references["product"]),
  type: text("type"),
  notes: text("notes"),
  aiDescription: text("aiDescription"),
});

export const generatedInventoryColumns = (
  references: Readonly<{
    ledgerParty: () => AnyPgColumn;
    location: () => AnyPgColumn;
    product: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<InventoryId>(),
  shortcode: text("shortcode").notNull(),
  productId: uuid("productId")
    .$type<ProductId>()
    .notNull()
    .references(references["product"]),
  amount: jsonb("amount").$type<Amount>().notNull(),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
  locationId: uuid("locationId")
    .$type<LocationId>()
    .notNull()
    .references(references["location"]),
  valuation: real("valuation"),
  verifiedAt: timestamp("verifiedAt", { mode: "date" }),
  placement: inventoryPlacementEnum("placement").notNull().default("stock"),
  ownershipMode: text("ownershipMode", { enum: inventoryOwnershipModeValues })
    .notNull()
    .default("inherit"),
  ownerLedgerPartyId: uuid("ownerLedgerPartyId")
    .$type<LedgerPartyId>()
    .references(references["ledgerParty"]),
});

export const generatedMealColumns = () => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<MealId>(),
  shortcode: text("shortcode").notNull(),
  date: date("date", { mode: "string" }).notNull(),
  name: text("name"),
  sortOrder: integer("sortOrder"),
  mealType: text("mealType", { enum: mealTypeValues }),
  mealKind: text("mealKind", { enum: mealKindValues })
    .notNull()
    .default("cooked"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedLedgerPartyColumns = () => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<LedgerPartyId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  kind: text("kind").$type<LedgerPartyKind>().notNull(),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedLedgerTransferColumns = (
  references: Readonly<{ ledgerParty: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<LedgerTransferId>(),
  shortcode: text("shortcode").notNull(),
  fromPartyId: uuid("fromPartyId")
    .$type<LedgerPartyId>()
    .notNull()
    .references(references["ledgerParty"]),
  toPartyId: uuid("toPartyId")
    .$type<LedgerPartyId>()
    .notNull()
    .references(references["ledgerParty"]),
  amount: doublePrecision("amount").notNull(),
  date: date("date", { mode: "string" }).notNull(),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedProjectColumns = (
  references: Readonly<{ project: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ProjectId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  status: text("status", { enum: projectStatusValues })
    .notNull()
    .default("planning"),
  kind: text("kind", { enum: projectKindValues }),
  locations: text("locations")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  locationsMode: text("locationsMode", { enum: ["inherit", "explicit"] })
    .notNull()
    .default("inherit"),
  defaultTrade: text("defaultTrade", { enum: tradeValues }),
  costEstimate: doublePrecision("costEstimate"),
  parentProjectId: uuid("parentProjectId")
    .$type<ProjectId>()
    .references(references["project"]),
  startDate: date("startDate", { mode: "string" }),
  endDate: date("endDate", { mode: "string" }),
  icon: text("icon"),
  notes: text("notes"),
  googleDriveFolderUrl: text("googleDriveFolderUrl"),
  notionPageUrl: text("notionPageUrl"),
  notionPageId: text("notionPageId"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedTaskColumns = (
  references: Readonly<{
    product: () => AnyPgColumn;
    project: () => AnyPgColumn;
    task: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<TaskId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  status: text("status", { enum: taskStatusValues })
    .notNull()
    .default("not_started"),
  projectId: uuid("projectId")
    .$type<ProjectId>()
    .references(references["project"]),
  projectMode: text("projectMode", { enum: ["inherit", "explicit"] })
    .notNull()
    .default("inherit"),
  subjectProductId: uuid("subjectProductId")
    .$type<ProductId>()
    .references(references["product"]),
  subjectProductMode: text("subjectProductMode", {
    enum: ["inherit", "explicit"],
  })
    .notNull()
    .default("inherit"),
  parentTaskId: uuid("parentTaskId")
    .$type<TaskId>()
    .references(references["task"]),
  dueDate: date("dueDate", { mode: "string" }),
  dueEndDate: date("dueEndDate", { mode: "string" }),
  trade: text("trade", { enum: tradeValues }),
  sortOrder: doublePrecision("sortOrder"),
  notionPageId: text("notionPageId"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedVendorColumns = () => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<VendorId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  website: text("website"),
  orderUrlTemplate: text("orderUrlTemplate"),
  orderEvidence: text("orderEvidence"),
  orderEmailSenders: text("orderEmailSenders")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  browserDomains: text("browserDomains")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  agentHints: jsonb("agentHints")
    .notNull()
    .default(
      sql`'{"ordersListUrl":null,"pagination":null,"orderLinkPattern":null,"notes":[]}'::jsonb`,
    ),
  returnWindowDays: integer("returnWindowDays"),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedPurchaseColumns = (
  references: Readonly<{
    project: () => AnyPgColumn;
    vendor: () => AnyPgColumn;
    vendorAccount: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<PurchaseId>(),
  shortcode: text("shortcode").notNull(),
  vendorId: uuid("vendorId")
    .$type<VendorId>()
    .notNull()
    .references(references["vendor"]),
  vendorAccountId: uuid("vendorAccountId")
    .$type<VendorAccountId>()
    .references(references["vendorAccount"]),
  defaultProjectId: uuid("defaultProjectId")
    .$type<ProjectId>()
    .references(references["project"]),
  defaultTrade: text("defaultTrade", { enum: tradeValues }),
  orderId: text("orderId"),
  displayLabel: text("displayLabel"),
  date: date("date", { mode: "string" }).notNull(),
  statedTotal: doublePrecision("statedTotal"),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedFinancialAccountColumns = (
  references: Readonly<{
    ledgerParty: () => AnyPgColumn;
    vendor: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<FinancialAccountId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  identity: jsonb("identity").$type<FinancialAccountIdentity>().notNull(),
  provisional: boolean("provisional").notNull().default(false),
  sourceAliases: jsonb("sourceAliases")
    .$type<FinancialAccountSourceAlias[]>()
    .notNull()
    .default([]),
  cardNumbers: jsonb("cardNumbers")
    .$type<FinancialAccountCardNumber[]>()
    .notNull()
    .default([]),
  providerVendorId: uuid("providerVendorId")
    .$type<VendorId>()
    .references(references["vendor"]),
  ledgerPartyId: uuid("ledgerPartyId")
    .$type<LedgerPartyId>()
    .references(references["ledgerParty"]),
  inventoryOwnerDefaultEnabled: boolean("inventoryOwnerDefaultEnabled")
    .notNull()
    .default(false),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedFinancialTransactionColumns = (
  references: Readonly<{
    financialAccount: () => AnyPgColumn;
    ledgerTransfer: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<FinancialTransactionId>(),
  shortcode: text("shortcode").notNull(),
  accountId: uuid("accountId")
    .$type<FinancialAccountId>()
    .notNull()
    .references(references["financialAccount"]),
  ledgerTransferId: uuid("ledgerTransferId")
    .$type<LedgerTransferId>()
    .references(references["ledgerTransfer"]),
  kind: text("kind").notNull(),
  status: text("status").notNull(),
  amount: doublePrecision("amount").notNull(),
  transactionDate: date("transactionDate", { mode: "string" }),
  postedDate: date("postedDate", { mode: "string" }),
  merchant: text("merchant"),
  rawDescription: text("rawDescription"),
  sourceCategory: text("sourceCategory"),
  sourceRefs: jsonb("sourceRefs")
    .$type<FinancialTransactionSourceRef[]>()
    .notNull()
    .default([]),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedWishColumns = () => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<WishId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  notes: text("notes"),
  acquiredAt: timestamp("acquiredAt", { mode: "date" }),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedExpenseColumns = (
  references: Readonly<{
    product: () => AnyPgColumn;
    project: () => AnyPgColumn;
    purchase: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ExpenseId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  cost: doublePrecision("cost"),
  date: date("date", { mode: "string" }),
  lineKind: text("lineKind", { enum: expenseLineKindValues })
    .notNull()
    .default("principal"),
  lineBasis: text("lineBasis", { enum: expenseLineBasisValues })
    .notNull()
    .default("item_line"),
  costType: text("costType", { enum: costTypeValues }).notNull(),
  trade: text("trade", { enum: tradeValues }),
  url: text("url"),
  notes: text("notes"),
  future: boolean("future").notNull().default(false),
  projectId: uuid("projectId")
    .$type<ProjectId>()
    .references(references["project"]),
  productId: uuid("productId")
    .$type<ProductId>()
    .references(references["product"]),
  productQuantity: doublePrecision("productQuantity"),
  purchaseId: uuid("purchaseId")
    .$type<PurchaseId>()
    .references(references["purchase"]),
  notionPageId: text("notionPageId"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedImageColumns = (
  references: Readonly<{ ledgerParty: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`),
  shortcode: text("shortcode").notNull(),
  key: text("key").notNull(),
  filename: text("filename").notNull(),
  size: integer("size").notNull(),
  contentType: text("contentType").notNull(),
  status: imageStatusEnum("status").notNull().default("PENDING"),
  width: integer("width"),
  height: integer("height"),
  perceptualHash: text("perceptualHash"),
  sourceFingerprint: jsonb(
    "sourceFingerprint",
  ).$type<ImageSourceFingerprint | null>(),
  detectedContentType: text("detectedContentType"),
  sha256: text("sha256"),
  renderStatus: imageRenderStatusEnum("renderStatus"),
  storageStatus: imageStorageStatusEnum("storageStatus"),
  source: text("source", { enum: ["own", "catalog", "unknown", "screenshot"] })
    .notNull()
    .default("unknown"),
  sourcePageUrl: text("sourcePageUrl"),
  sourceAssetUrl: text("sourceAssetUrl"),
  sourceName: text("sourceName"),
  useOriginal: boolean("useOriginal").notNull().default(false),
  verifiedAt: timestamp("verifiedAt", { mode: "date" }),
  capturedAt: timestamp("capturedAt", { mode: "date" }),
  capturedAtOffsetMinutes: integer("capturedAtOffsetMinutes"),
  captureLocation: jsonb(
    "captureLocation",
  ).$type<ImageCaptureLocation | null>(),
  capturePlaceName: text("capturePlaceName"),
  captureDeviceLabel: text("captureDeviceLabel"),
  capturedByPartyId: uuid("capturedByPartyId")
    .$type<LedgerPartyId>()
    .references(references["ledgerParty"]),
  captureAttribution: text("captureAttribution", {
    enum: ["none", "derived", "ambiguous", "confirmed"],
  })
    .notNull()
    .default("none"),
  provenanceEvidence: jsonb(
    "provenanceEvidence",
  ).$type<ImageProvenanceEvidence | null>(),
  metadataRevision: integer("metadataRevision"),
  embeddedMetadata: jsonb(
    "embeddedMetadata",
  ).$type<StoredImageEmbeddedMetadata | null>(),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedPlantingColumns = (
  references: Readonly<{
    location: () => AnyPgColumn;
    plant: () => AnyPgColumn;
    product: () => AnyPgColumn;
    task: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`),
  shortcode: text("shortcode").notNull(),
  plantId: uuid("plantId")
    .$type<PlantId>()
    .notNull()
    .references(references["plant"]),
  outcome: text("outcome"),
  sourceProductId: uuid("sourceProductId")
    .$type<ProductId>()
    .references(references["product"]),
  locationId: uuid("locationId")
    .$type<LocationId>()
    .references(references["location"]),
  taskId: uuid("taskId").$type<TaskId>().references(references["task"]),
  status: text("status").notNull().default("planned"),
  quantity: text("quantity"),
  notes: text("notes"),
  plannedWindow: text("plannedWindow"),
  sowedOn: date("sowedOn", { mode: "string" }),
  transplantedOn: date("transplantedOn", { mode: "string" }),
  finishedOn: date("finishedOn", { mode: "string" }),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedGardenEntryColumns = (
  references: Readonly<{ location: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`),
  shortcode: text("shortcode").notNull(),
  locationId: uuid("locationId")
    .$type<LocationId>()
    .notNull()
    .references(references["location"]),
  kind: text("kind").notNull().default("note"),
  observedOn: date("observedOn", { mode: "string" }).notNull(),
  note: text("note"),
  harvestAmount: text("harvestAmount"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedVendorAccountColumns = (
  references: Readonly<{
    ledgerParty: () => AnyPgColumn;
    vendor: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<VendorAccountId>(),
  shortcode: text("shortcode").notNull(),
  label: text("label").notNull(),
  vendorId: uuid("vendorId")
    .$type<VendorId>()
    .notNull()
    .references(references["vendor"]),
  ledgerPartyId: uuid("ledgerPartyId")
    .$type<LedgerPartyId>()
    .notNull()
    .references(references["ledgerParty"]),
  inventoryOwnerDefaultEnabled: boolean("inventoryOwnerDefaultEnabled")
    .notNull()
    .default(false),
  status: text("status").notNull().default("active"),
  browser: text("browser").notNull().default("chrome"),
  cursor: jsonb("cursor")
    .notNull()
    .default(
      sql`'{"newestOrderAt":null,"orderIdsOnNewestDate":[],"backfillBeforeOrderAt":null,"earliestAvailableOrderAt":null}'::jsonb`,
    ),
  lastRunAt: timestamp("lastRunAt", { mode: "date" }),
  lastSuccessAt: timestamp("lastSuccessAt", { mode: "date" }),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedProductCategoryColumns = (
  references: Readonly<{ productCategory: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ProductCategoryId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  aliases: text("aliases")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  description: text("description"),
  parentId: uuid("parentId")
    .$type<ProductCategoryId>()
    .references(references["productCategory"]),
  sortOrder: integer("sortOrder").notNull().default(0),
  feature: text("feature", { enum: productCategoryFeatureValues }),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedImportRunColumns = (
  references: Readonly<{
    ledgerParty: () => AnyPgColumn;
    vendor: () => AnyPgColumn;
    vendorAccount: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ImportRunId>(),
  shortcode: text("shortcode").notNull(),
  ledgerPartyId: uuid("ledgerPartyId")
    .$type<LedgerPartyId>()
    .references(references["ledgerParty"]),
  actorName: text("actorName").notNull(),
  vendorAccountId: uuid("vendorAccountId")
    .$type<VendorAccountId>()
    .references(references["vendorAccount"]),
  vendorId: uuid("vendorId").$type<VendorId>().references(references["vendor"]),
  purpose: text("purpose").notNull().default("account_sync"),
  trigger: text("trigger").notNull(),
  status: text("status").notNull().default("running"),
  coordinatorModel: text("coordinatorModel").notNull().default("gpt-6-sol"),
  skillRevision: text("skillRevision").notNull().default("purchase-import@1"),
  runtimeRevision: text("runtimeRevision").notNull().default("flue@1"),
  decisionRevision: integer("decisionRevision").notNull().default(1),
  startedAt: timestamp("startedAt", { mode: "date" }).notNull().defaultNow(),
  endedAt: timestamp("endedAt", { mode: "date" }),
  ordersSeen: integer("ordersSeen").notNull().default(0),
  imported: integer("imported").notNull().default(0),
  updated: integer("updated").notNull().default(0),
  skipped: integer("skipped").notNull().default(0),
  auditedAt: timestamp("auditedAt", { mode: "date" }),
  failureCode: text("failureCode"),
  notes: text("notes"),
  dispatchAttempts: integer("dispatchAttempts").notNull().default(0),
  dispatchError: text("dispatchError"),
  coordinatorStartedAt: timestamp("coordinatorStartedAt", { mode: "date" }),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedDeviceColumns = (
  references: Readonly<{
    ledgerParty: () => AnyPgColumn;
    product: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<DeviceId>(),
  shortcode: text("shortcode").notNull(),
  installationId: text("installationId").notNull(),
  name: text("name").notNull(),
  platform: text("platform").notNull(),
  appVersion: text("appVersion"),
  osVersion: text("osVersion"),
  lastSeenAt: timestamp("lastSeenAt", { mode: "date" }),
  automaticWork: boolean("automaticWork").notNull().default(true),
  remotePaused: boolean("remotePaused").notNull().default(false),
  ledgerPartyId: uuid("ledgerPartyId")
    .$type<LedgerPartyId>()
    .references(references["ledgerParty"]),
  productId: uuid("productId")
    .$type<ProductId>()
    .references(references["product"]),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedImageSightingColumns = (
  references: Readonly<{
    device: () => AnyPgColumn;
    image: () => AnyPgColumn;
    ledgerParty: () => AnyPgColumn;
  }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<ImageSightingId>(),
  shortcode: text("shortcode").notNull(),
  imageId: uuid("imageId").notNull().references(references["image"]),
  ledgerPartyId: uuid("ledgerPartyId")
    .$type<LedgerPartyId>()
    .notNull()
    .references(references["ledgerParty"]),
  deviceId: uuid("deviceId")
    .$type<DeviceId>()
    .notNull()
    .references(references["device"]),
  assetKey: text("assetKey").notNull(),
  cloudIdentifier: text("cloudIdentifier"),
  localIdentifier: text("localIdentifier"),
  sourceType: text("sourceType", {
    enum: ["userLibrary", "cloudShared", "iTunesSynced"],
  }).notNull(),
  mediaSubtypes: text("mediaSubtypes")
    .array()
    .notNull()
    .default(sql`'{}'::text[]`),
  originalFilename: text("originalFilename"),
  pixelWidth: integer("pixelWidth"),
  pixelHeight: integer("pixelHeight"),
  hasAdjustments: boolean("hasAdjustments").notNull().default(false),
  capturedAt: timestamp("capturedAt", { mode: "date" }),
  capturedAtOffsetMinutes: integer("capturedAtOffsetMinutes"),
  addedAt: timestamp("addedAt", { mode: "date" }),
  location: jsonb("location").$type<ImageSightingLocation | null>(),
  placeName: text("placeName"),
  camera: jsonb("camera").$type<ImageSightingCamera | null>(),
  matchKind: text("matchKind", { enum: ["import", "libraryMatch"] }).notNull(),
  hashDistance: integer("hashDistance"),
  aspectGate: boolean("aspectGate"),
  observedAt: timestamp("observedAt", { mode: "date" }).notNull(),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});

export const generatedPlantColumns = (
  references: Readonly<{ ingredient: () => AnyPgColumn }>,
) => ({
  id: uuid("id")
    .primaryKey()
    .default(sql`gen_random_uuid()`)
    .$type<PlantId>(),
  shortcode: text("shortcode").notNull(),
  name: text("name").notNull(),
  gardenGuideKey: text("gardenGuideKey"),
  verdict: text("verdict"),
  ingredientId: uuid("ingredientId")
    .$type<IngredientId>()
    .references(references["ingredient"]),
  latinName: text("latinName"),
  breeding: text("breeding"),
  daysFromSowMin: integer("daysFromSowMin"),
  daysFromSowMax: integer("daysFromSowMax"),
  daysFromTransplantMin: integer("daysFromTransplantMin"),
  daysFromTransplantMax: integer("daysFromTransplantMax"),
  notes: text("notes"),
  createdAt: timestamp("createdAt", { mode: "date" }).notNull().defaultNow(),
  updatedAt: timestamp("updatedAt", { mode: "date" })
    .notNull()
    .defaultNow()
    .$onUpdate(() => new Date()),
  deletedAt: timestamp("deletedAt", { mode: "date" }),
});
