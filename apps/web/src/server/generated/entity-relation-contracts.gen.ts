// Generated by `pnpm entity:generate` from `scripts/entity-literals/entities/*.entity.ts`. Do not edit.

// Entity artifact hashes: source=49370b35e82614b6 content=c7afad7eddc8b0b1
import { relationMutationOut } from "@cubby/schemas/common";
import type { ShortcodeEntity } from "@cubby/schemas/entity-manifest";
import { shortcodeSchema } from "@cubby/schemas/identifiers";
import { entityRelationReferenceItemSchema } from "@cubby/schemas/common";
import { productComponentRelationItemSchema } from "@cubby/schemas/common";
import { z } from "zod";

// Generated relation command schemas stay correlated by entity and relation.
const generatedEntityRelationCommandSchema = z.union([
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("product"),
      relation: z.literal("components"),
      id: shortcodeSchema("product"),
      items: z
        .array(
          productComponentRelationItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("project"),
      relation: z.literal("resources"),
      id: shortcodeSchema("project"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("purchase"),
      relation: z.literal("products"),
      id: shortcodeSchema("purchase"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
]);
export const generatedBrowserEntityRelationCommandSchema = z.union([
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("product"),
      relation: z.literal("components"),
      id: shortcodeSchema("product"),
      items: z
        .array(
          productComponentRelationItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("project"),
      relation: z.literal("resources"),
      id: shortcodeSchema("project"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("purchase"),
      relation: z.literal("products"),
      id: shortcodeSchema("purchase"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
]);
export const generatedMcpEntityRelationCommandSchema = z.union([
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("product"),
      relation: z.literal("components"),
      id: shortcodeSchema("product"),
      items: z
        .array(
          productComponentRelationItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("project"),
      relation: z.literal("resources"),
      id: shortcodeSchema("project"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
  z
    .object({
      action: z.enum(["attach", "detach"]),
      entity: z.literal("purchase"),
      relation: z.literal("products"),
      id: shortcodeSchema("purchase"),
      items: z
        .array(
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        )
        .min(1)
        .max(500),
    })
    .strict(),
]);

export const generatedMcpEntityRelationPreviewInputSchema = z
  .object({
    action: z.enum(["attach", "detach"]),
    entity: z.enum(["product", "project", "purchase"]),
    relation: z.enum(["components", "resources", "products"]),
    id: z.union([
      shortcodeSchema("product"),
      shortcodeSchema("project"),
      shortcodeSchema("purchase"),
    ]),
    items: z
      .array(
        z.union([
          productComponentRelationItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
          entityRelationReferenceItemSchema.extend({
            id: shortcodeSchema("product"),
          }),
        ]),
      )
      .min(1)
      .max(500),
  })
  .strict()
  .superRefine((input, ctx) => {
    const result = generatedMcpEntityRelationCommandSchema.safeParse(input);
    if (!result.success) {
      for (const issue of result.error.issues)
        ctx.addIssue({
          code: "custom",
          path: issue.path,
          message: issue.message,
        });
    }
  });

export const generatedEntityRelationMutationResultSchema = z.union([
  z.object({
    action: z.enum(["attach", "detach"]),
    entity: z.literal("product"),
    relation: z.literal("components"),
    result: relationMutationOut,
  }),
  z.object({
    action: z.enum(["attach", "detach"]),
    entity: z.literal("project"),
    relation: z.literal("resources"),
    result: relationMutationOut,
  }),
  z.object({
    action: z.enum(["attach", "detach"]),
    entity: z.literal("purchase"),
    relation: z.literal("products"),
    result: relationMutationOut,
  }),
]);

export type GeneratedEntityRelationCommand = z.infer<
  typeof generatedEntityRelationCommandSchema
>;

/** Resolve generated relation metadata without a parallel hand-written roster. */
export function generatedEntityRelationTarget(
  command: GeneratedEntityRelationCommand,
): ShortcodeEntity {
  switch (`${command.entity}:${command.relation}`) {
    case "product:components":
      return "product";
    case "project:resources":
      return "product";
    case "purchase:products":
      return "product";
  }
  throw new Error(`Unsupported relation ${command.entity}:${command.relation}`);
}

export const generatedEntityRelationItemIds = (
  command: GeneratedEntityRelationCommand,
): string[] => command.items.map((item) => item.id);
