// Generated by `pnpm generate` — do not edit.

// Artifact hashes: source=a7e82f72421a1333 content=a7e82f72421a1333
import { createFileRoute, stripSearchParams } from "@tanstack/react-router";

import { CreateDialogAction } from "~/app/_components/forms/create-dialog-action";
import { listPage } from "~/app/_components/routing/entity-routes";
import { captureRequest } from "~/entities/editing/editor-requests";
import { entityListLoader } from "~/entities/entity-list-ssr";
import { entitySearch } from "~/entities/generated/entity-search.gen";
import { pageTitle } from "~/lib/page-title";

// Bound to a const, not inlined into the options object: the router plugin's
// splitter re-parses an inlined call expression with a JSX-less babel config,
// so only the identifier path survives a page body that renders JSX.
const LocationsPage = listPage({
  entity: "location",
  actions: () => <CreateDialogAction request={captureRequest("location")} />,
});

export const Route = createFileRoute("/_authenticated/locations/")({
  validateSearch: entitySearch.location.schema,
  search: { middlewares: [stripSearchParams(entitySearch.location.defaults)] },
  loaderDeps: ({ search }) => search,
  loader: entityListLoader("location"),
  head: () => ({ meta: [{ title: pageTitle("Locations") }] }),
  component: LocationsPage,
});
