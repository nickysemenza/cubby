// The CubbyAPI client is generated at build time by swift-openapi-generator's
// `OpenAPIGenerator` plugin (see README.md). SwiftPM treats a target with no
// Swift file of its own as having no sources and never links the plugin's
// output, so this file must exist.
