// Generated by apps/web/scripts/generate-http-openapi.ts — do not edit.
// Regenerate with `pnpm --filter @cubby/web generate:http-api`; `pnpm check` fails when stale.

import CubbyAPI

/// Thrown when a catalog entity has no HTTP route for the requested action.
public enum EntityOperationError: Error, Sendable, Hashable {
    case unsupported(EntityKey, EntityAction)
}

extension EntityKey {
    /// The resource actions the HTTP document actually exposes for this entity. Authoritative
    /// over `EntityDescriptor.actions`, which comes from the kernel roster and can declare an
    /// action that has no HTTP route.
    public var httpActions: Set<EntityAction> {
        switch self {
        case .expense: [.create, .delete, .get, .list, .update]
        case .financialAccount: [.create, .delete, .get, .list, .update]
        case .financialTransaction: [.create, .delete, .get, .list, .update]
        case .image: [.delete, .update]
        case .ingredient: [.create, .delete, .get, .list, .update]
        case .inventory: [.create, .delete, .get, .list, .update]
        case .ledgerParty: [.create, .delete, .get, .list, .update]
        case .ledgerTransfer: [.create, .delete, .get, .list, .update]
        case .location: [.create, .delete, .get, .list, .update]
        case .meal: [.create, .delete, .get, .list, .update]
        case .product: [.create, .delete, .get, .list, .update]
        case .project: [.create, .delete, .get, .list, .update]
        case .purchase: [.create, .delete, .get, .list, .update]
        case .recipe: [.create, .delete, .get, .list, .update]
        case .task: [.create, .delete, .get, .list, .update]
        case .vendor: [.create, .delete, .get, .list, .update]
        case .wish: [.create, .delete, .get, .list, .update]
        default: []
        }
    }
}

extension EntityDescriptor {
    /// One page of rows as the dynamic projection `EntityRow` reads. Typed on the wire; the
    /// `JSONValue` is produced from the decoded value, never from the response bytes.
    func listPage(client: Client, page: Int, pageSize: Int, sort: String?) async throws -> ListPage<JSONValue> {
        switch key {
        case .expense:
            let page = try await client.resources_expense_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .financialAccount:
            let page = try await client.resources_financialAccount_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .financialTransaction:
            let page = try await client.resources_financialTransaction_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .ingredient:
            let page = try await client.resources_ingredient_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .inventory:
            let page = try await client.resources_inventory_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .ledgerParty:
            let page = try await client.resources_ledgerParty_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .ledgerTransfer:
            let page = try await client.resources_ledgerTransfer_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .location:
            let page = try await client.resources_location_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .meal:
            let page = try await client.resources_meal_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .product:
            let page = try await client.resources_product_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .project:
            let page = try await client.resources_project_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .purchase:
            let page = try await client.resources_purchase_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .recipe:
            let page = try await client.resources_recipe_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .task:
            let page = try await client.resources_task_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .vendor:
            let page = try await client.resources_vendor_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        case .wish:
            let page = try await client.resources_wish_list(
                query: .init(page: page, pageSize: pageSize, sort: sort)
            ).ok.body.json
            return ListPage(
                items: try page.items.map(JSONValue.init(encoding:)),
                meta: PageMeta(page.meta)
            )
        default: throw EntityOperationError.unsupported(key, .list)
        }
    }

    /// One row by id, as the dynamic projection `EntityRow` reads.
    func getRow(client: Client, id: String) async throws -> JSONValue {
        switch key {
        case .expense:
            return try JSONValue(encoding: try await client.resources_expense_get(path: .init(id: id)).ok.body.json)
        case .financialAccount:
            return try JSONValue(encoding: try await client.resources_financialAccount_get(path: .init(id: id)).ok.body.json)
        case .financialTransaction:
            return try JSONValue(encoding: try await client.resources_financialTransaction_get(path: .init(id: id)).ok.body.json)
        case .ingredient:
            return try JSONValue(encoding: try await client.resources_ingredient_get(path: .init(id: id)).ok.body.json)
        case .inventory:
            return try JSONValue(encoding: try await client.resources_inventory_get(path: .init(id: id)).ok.body.json)
        case .ledgerParty:
            return try JSONValue(encoding: try await client.resources_ledgerParty_get(path: .init(id: id)).ok.body.json)
        case .ledgerTransfer:
            return try JSONValue(encoding: try await client.resources_ledgerTransfer_get(path: .init(id: id)).ok.body.json)
        case .location:
            return try JSONValue(encoding: try await client.resources_location_get(path: .init(id: id)).ok.body.json)
        case .meal:
            return try JSONValue(encoding: try await client.resources_meal_get(path: .init(id: id)).ok.body.json)
        case .product:
            return try JSONValue(encoding: try await client.resources_product_get(path: .init(id: id)).ok.body.json)
        case .project:
            return try JSONValue(encoding: try await client.resources_project_get(path: .init(id: id)).ok.body.json)
        case .purchase:
            return try JSONValue(encoding: try await client.resources_purchase_get(path: .init(id: id)).ok.body.json)
        case .recipe:
            return try JSONValue(encoding: try await client.resources_recipe_get(path: .init(id: id)).ok.body.json)
        case .task:
            return try JSONValue(encoding: try await client.resources_task_get(path: .init(id: id)).ok.body.json)
        case .vendor:
            return try JSONValue(encoding: try await client.resources_vendor_get(path: .init(id: id)).ok.body.json)
        case .wish:
            return try JSONValue(encoding: try await client.resources_wish_get(path: .init(id: id)).ok.body.json)
        default: throw EntityOperationError.unsupported(key, .get)
        }
    }

    /// `resources.<key>.update` with only `pendingImageIds` set, for the entities whose update
    /// body declares it (the same set `OperationRoute.imageAttachableEntities` lists).
    func attachImages(_ imageIds: [String], to id: String, client: Client) async throws {
        switch key {
        case .location:
            _ = try await client.resources_location_update(
                path: .init(id: id), body: .json(.init(pendingImageIds: imageIds))
            ).ok
        case .product:
            _ = try await client.resources_product_update(
                path: .init(id: id), body: .json(.init(pendingImageIds: imageIds))
            ).ok
        case .purchase:
            _ = try await client.resources_purchase_update(
                path: .init(id: id), body: .json(.init(pendingImageIds: imageIds))
            ).ok
        case .recipe:
            _ = try await client.resources_recipe_update(
                path: .init(id: id), body: .json(.init(pendingImageIds: imageIds))
            ).ok
        default: throw EntityOperationError.unsupported(key, .update)
        }
    }

    /// `resources.<key>.update` with only `imageOrder` set.
    func setImageOrder(_ imageIds: [String], on id: String, client: Client) async throws {
        switch key {
        case .location:
            _ = try await client.resources_location_update(
                path: .init(id: id), body: .json(.init(imageOrder: imageIds))
            ).ok
        case .product:
            _ = try await client.resources_product_update(
                path: .init(id: id), body: .json(.init(imageOrder: imageIds))
            ).ok
        case .purchase:
            _ = try await client.resources_purchase_update(
                path: .init(id: id), body: .json(.init(imageOrder: imageIds))
            ).ok
        case .recipe:
            _ = try await client.resources_recipe_update(
                path: .init(id: id), body: .json(.init(imageOrder: imageIds))
            ).ok
        default: throw EntityOperationError.unsupported(key, .update)
        }
    }
}
