{
  "name": "cubby",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "agent:setup": "node scripts/setup-agent-environment.mjs",
    "dev": "pnpm -r dev",
    "dev:log": "pnpm -r --reporter append-only dev",
    "build": "node scripts/with-slot.mjs pnpm -r --workspace-concurrency=2 build",
    "check": "node scripts/with-slot.mjs pnpm run check:all",
    "check:all": "biome check --error-on-warnings && pnpm typecheck && pnpm types:check && pnpm openapi:check && pnpm knip && pnpm dedupe:check && pnpm audit:security && node --test scripts/ci-scope.test.mjs && node scripts/check-conventions.mjs && node scripts/check-soft-delete-filters.mjs",
    "typecheck": "node scripts/with-slot.mjs pnpm -r --parallel --workspace-concurrency=4 typecheck",
    "types:check": "pnpm --filter @cubby/web --filter @cubby/upc-lookup --filter @cubby/usda-api --parallel types:check",
    "openapi:check": "pnpm --filter @cubby/usda-api generate:openapi:check",
    "knip": "knip --no-config-hints",
    "dedupe:check": "pnpm dedupe --check",
    "docs:check": "node scripts/check-markdown-links.mjs",
    "audit:security": "node scripts/check-security-audit.mjs",
    "lint": "pnpm -r lint",
    "format:check": "pnpm -r format:check",
    "format:write": "pnpm -r format:write",
    "test": "node scripts/with-slot.mjs pnpm -r --workspace-concurrency=2 test",
    "test-ui": "pnpm --filter @cubby/web test:ui",
    "test:e2e": "pnpm -r test:e2e",
    "wasm": "CARGO_TARGET_DIR=\"${CARGO_TARGET_DIR:-$HOME/.cache/cubby/recipebridge-target}\" wasm-pack build --scope cubby --out-dir ../packages/wasm recipebridge",
    "deploy:all": "pnpm --filter @cubby/usda-api run deploy && pnpm --filter @cubby/upc-lookup run deploy && pnpm --filter @cubby/web run deploy:cf",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "husky": "^9.1.7",
    "knip": "^6.27.0",
    "typescript": "^7.0.2"
  }
}
