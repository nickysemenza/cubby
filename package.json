{
  "name": "cubby",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "agent:setup": "node scripts/setup-agent-environment.ts",
    "dev": "pnpm -r dev",
    "dev:log": "pnpm -r --reporter append-only dev",
    "build": "pnpm -r build",
    "check": "concurrently --kill-others-on-fail --max-processes 6 --names entity,types,biome,scripts,sql,soft-delete \"pnpm entity:check\" \"pnpm typecheck\" \"pnpm format:check:changed\" \"pnpm typecheck:scripts\" \"node scripts/check-sql-safety.ts\" \"node scripts/check-soft-delete-filters.ts\"",
    "check:all": "concurrently --kill-others-on-fail --max-processes 4 --names core,biome,knip,bindings,openapi,ci-scope,security \"pnpm check\" \"biome check --error-on-warnings\" \"pnpm knip\" \"pnpm types:check\" \"pnpm openapi:check\" \"node --test scripts/ci-scope.test.ts\" \"pnpm audit:security\"",
    "typecheck:scripts": "tsc --noEmit -p tsconfig.json",
    "typecheck": "pnpm -r --parallel typecheck",
    "typecheck:web": "pnpm --filter @cubby/web typecheck",
    "types:check": "pnpm --filter @cubby/web --filter @cubby/upc-lookup --filter @cubby/usda-api --parallel types:check",
    "openapi:check": "pnpm --filter @cubby/usda-api generate:openapi:check",
    "entity:generate": "node scripts/entity-literal-generator.ts",
    "entity:check": "node scripts/entity-literal-generator.ts --check",
    "knip": "knip --no-config-hints",
    "dedupe:check": "pnpm dedupe --check",
    "docs:check": "node scripts/check-markdown-links.ts",
    "audit:security": "node scripts/check-security-audit.ts",
    "lint": "pnpm -r lint",
    "format:check": "pnpm -r format:check",
    "format:write": "pnpm -r format:write",
    "format:changed": "node scripts/format-changed.ts",
    "format:check:changed": "node scripts/format-changed.ts --check",
    "test": "pnpm -r test",
    "test:unit": "pnpm --filter @cubby/web test:unit",
    "test:unit-pure": "pnpm --filter @cubby/web test:unit-pure",
    "test:ui": "pnpm --filter @cubby/web test:ui",
    "test:pglite": "pnpm --filter @cubby/web test:pglite",
    "test:integration": "pnpm --filter @cubby/web test:integration",
    "test:changed": "pnpm --filter @cubby/web test:changed",
    "test:file": "pnpm --filter @cubby/web test:file",
    "test-ui": "pnpm --filter @cubby/web test:watch",
    "test:e2e": "pnpm -r test:e2e",
    "wasm": "CARGO_TARGET_DIR=\"${CARGO_TARGET_DIR:-$HOME/.cache/cubby/recipebridge-target}\" wasm-pack build --scope cubby --out-dir ../packages/wasm recipebridge",
    "deploy:all": "pnpm --filter @cubby/usda-api run deploy && pnpm --filter @cubby/upc-lookup run deploy && pnpm --filter @cubby/web run deploy:cf",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.5",
    "husky": "^9.1.7",
    "knip": "^6.27.0",
    "oxc-parser": "0.147.0",
    "typescript": "^7.0.2"
  }
}
