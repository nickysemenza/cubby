{
  "env": {
    "CLAUDE_CODE_SUBAGENT_MODEL": "sonnet"
  },
  "model": "opus",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "sh",
            "args": [
              "-c",
              "if [ \"$(git -C \"$CLAUDE_PROJECT_DIR\" rev-parse --path-format=absolute --git-dir)\" != \"$(git -C \"$CLAUDE_PROJECT_DIR\" rev-parse --path-format=absolute --git-common-dir)\" ]; then pnpm --dir \"$CLAUDE_PROJECT_DIR\" run agent:setup; fi"
            ],
            "timeout": 600
          }
        ]
      }
    ]
  },
  "effortLevel": "medium",
  "enabledPlugins": {
    "axiom@axiom-marketplace": true
  }
}
