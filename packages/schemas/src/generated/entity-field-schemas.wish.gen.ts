// Generated by `pnpm entity:generate` from `scripts/entity-literals/entities/*.entity.ts`. Do not edit.

// Entity artifact hashes: source=3cfa71b0b6d85548 content=8f1c441fe8e241a9
import { productShortcode, wishShortcode } from "@cubby/schemas/identifiers";
import { wishCandidateOut } from "@cubby/schemas/wish-fields";
import { z } from "zod";

export const generatedWishFieldSchemas = {
  create: {
    name: z.string().trim().min(1).max(200),
    notes: z.string().nullable().default(null),
    candidateProductIds: z.array(productShortcode).default([]),
  },
  update: {
    name: z.string().trim().min(1).max(200).optional(),
    notes: z.string().nullable().optional(),
    candidateProductIds: z.array(productShortcode).optional(),
    acquired: z.boolean().optional(),
  },
  read: {
    id: wishShortcode,
    name: z.string().trim().min(1).max(200),
    notes: z.string().nullable(),
    acquiredAt: z.date().nullable(),
    candidates: z.array(wishCandidateOut),
    createdAt: z.date(),
    updatedAt: z.date(),
  },
} as const;
export const generatedWishFilterFields = {
  search: z.string().trim().min(1).max(200).optional(),
  acquired: z.boolean().optional(),
} as const;
