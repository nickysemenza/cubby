#!/usr/bin/env sh
set -e

# Validate staged files without rewriting the working tree. This preserves the
# user's staged/unstaged split for partially staged files.
pnpm check:staged

# Native TypeScript is now cheap enough that selective orchestration costs more
# maintenance than it saves.
pnpm typecheck
