{
  "git": {
    "deploymentEnabled": false
  },
  "regions": ["pdx1"],
  "framework": null,
  "installCommand": "corepack enable && corepack prepare pnpm@10.26.0 --activate && pnpm install",
  "buildCommand": "pnpm db:migrate && pnpm build",
  "outputDirectory": "apps/web/.vercel/output"
}
