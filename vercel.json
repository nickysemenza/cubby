{
  "git": {
    "deploymentEnabled": false
  },
  "regions": ["pdx1"],
  "framework": null,
  "installCommand": "corepack enable && corepack prepare pnpm@10.17.1 --activate && pnpm install",
  "buildCommand": "pnpm db:migrate && pnpm build",
  "outputDirectory": "apps/web/.vercel/output"
}
